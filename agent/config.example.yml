services:
  ssh:
    enabled: true
    port: 2222
    banner: "Welcome to Internal Secure Server v2.1"
    host_key: "host.key"
  http:
    enabled: false
    port: 80

logging:
  remote_logging: true
  verbosity: high
  controller_url: "http://controller:5000/api/logs"
